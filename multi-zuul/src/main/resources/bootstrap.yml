spring:
  application:
    name: multi-zuul-server
server:
  port: 1310
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka3000.com:3000/eureka/
  instance:
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
    prefer-ip-address: true
    instance-id: zuul-1
